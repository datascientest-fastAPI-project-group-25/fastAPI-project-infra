{
  "name": "github-actions-utils",
  "version": "1.0.0",
  "description": "Helper utilities for GitHub Actions workflows",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist",
    "lint": "eslint src/**/*.ts",
    "format": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "@aws-sdk/client-iam": "^3.515.0",
    "@aws-sdk/client-s3": "^3.515.0",
    "@aws-sdk/client-sts": "^3.515.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.19",
    "bun-types": "latest",
    "eslint": "^8.56.0",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}