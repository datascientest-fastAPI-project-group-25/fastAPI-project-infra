# Utility Scripts

This directory contains utility scripts for various tasks.

## Scripts

- `create-state-resources.sh`: Create S3 bucket and DynamoDB table for Terraform state
- `setup-state.sh`: Set up Terraform state resources

## Usage

These scripts should be run from the root of the repository:

```bash
./scripts/utils/setup-state.sh
```
