# Testing Scripts

This directory contains scripts for testing the infrastructure.

## Scripts

- `checkov.sh`: Run Checkov security scanning on Terraform code
- `init-tflint.sh`: Initialize TFLint for Terraform linting

## Usage

These scripts should be run from the root of the repository:

```bash
./scripts/testing/checkov.sh
```
