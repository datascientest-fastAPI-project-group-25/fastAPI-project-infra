# Scripts Directory

This directory contains scripts for deploying, testing, and managing the infrastructure.

## Directory Structure

- `deployment/`: Scripts for deploying infrastructure
- `testing/`: Scripts for testing infrastructure
- `utils/`: Utility scripts for various tasks

## Usage

Most scripts should be run from the root of the repository. For example:

```bash
./scripts/deployment/deploy.sh
```
