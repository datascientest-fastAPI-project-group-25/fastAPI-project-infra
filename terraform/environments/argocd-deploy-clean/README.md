# ArgoCD Clean Deploy

This directory contains an experimental configuration for deploying ArgoCD separately from the main infrastructure. It is not currently used in production.

## Purpose

This configuration was created to test deploying ArgoCD in a clean, isolated environment before integrating it with the main infrastructure.

## Status

This is an experimental configuration and should not be used for production deployments. Please use the configurations in the `clean-deploy` directory instead.
